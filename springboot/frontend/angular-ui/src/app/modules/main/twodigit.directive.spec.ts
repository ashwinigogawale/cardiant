import { TwodigitDirective } from './twodigit.directive';

describe('TwodigitDirective', () => {
  it('should create an instance', () => {
    const directive = new TwodigitDirective();
    expect(directive).toBeTruthy();
  });
});
